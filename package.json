{
  "name": "papercut",
  "version": "1.0.0",
  "private": true,
  "description": "PaperCut document-to-roadmap platform",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check lib/vercel-api.js && node --check server.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0",
    "mammoth": "^1.9.0",
    "pdf-parse": "1.1.1"
  }
}
